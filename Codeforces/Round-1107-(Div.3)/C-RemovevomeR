#include <bits/stdc++.h>
using namespace std;
int main() {
    int t;
    cin >> t;
    for (int i = 0; i < t; ++i) {
        int n;
        string s;
        cin>>n>>s;
        int blocks = 1;
            for (int j = 0; j < n - 1; j++) {
                if (s[j] != s[j + 1]) {
                    blocks++;
                    }
            }
         if (blocks == 2) {
                cout << 2 << "\n";
            } else {
                cout << 1 << "\n";
            }
 
        }
}
